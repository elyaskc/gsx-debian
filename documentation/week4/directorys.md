# Directorios de trabajo de Greendevcorp

Se han creado las carpetas /bin, /shared y el archivo done.log bajo el directorio /home/greendevcorp.

En la carpeta /bin:
	El dueño tiene permiso de lectura, escritura y ejecución, los del grupo solo ejecución y lectura, el resto nada.

En la carpeta /shared:
	El dueño y los del grupo tienen todos los permisos (rwx) y el resto nada.
	Además se ha activado el sticky bit y el gid.

En el archivo done.log:
	Se ha asignado a dev1 como dueño.
	Solo el dueño tiene permiso de lectura y escritura, los del grupo solo lectura y el resto nada.
